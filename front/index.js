//hola no se que hcaer xd lol

function register() { // @igna tenes que hacer el appi de agregar un usuario a la bd

    let user = ui.getUser()
    let fullName = ui.getFullName()
    let password = ui.getPassword()
    let id = ui.getId()

    fetch //aca va la funcion post app que tenes que hacer @igna pasado por parametro el user, fullName (nombre y apellido juntos), password, y el id que es el dni

}
function fetchCheckUser(id, user, password) {
    fetch //aca va la funcion post app que tenes que hacer @igna pasado por parametro el user, password, y el id que es el dni para verificar que exista el usuario y que sea correcto
}
function logIn() { //necesito un appi que verifique que exista este usuario con este password @igna
    let id = ui.getId()
    let user = ui.getUser()
    let password = ui.getPassword()
    if (fetchCheckUser(id, user, password)) {

    }



}
